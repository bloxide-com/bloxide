# Documentation & Consistency Cleanup Plan

## Group A: Top-Level Entry-Point Docs

### README.md
1. Fix broken `START_HERE.md` link (line 25) → point to `AGENTS.md`
2. Update `bloxide-macros/` description (line 82) → remove `#[derive(StateTopology)]`, `event!`
3. Update crates table (line 133) → same
4. Add `crates/tools/` and `tools/` to repository layout diagram
5. Add a "Building with `cargo blox`" section mentioning `blox.toml` + `cargo blox generate`

### AGENTS.md
1. Add `crates/tools/bloxide-codegen`, `crates/tools/cargo-blox`, `tools/bloxide-viz-export`, `tools/bloxide-visualizer` to layout
2. Add missing architecture docs 14-17 to "Where to Find Things" table
3. Fix invariant #15: remove `#[ctor]` reference, use "constructor fields (auto-detected by naming convention)"
4. Fix clarification section: remove `#[ctor]` references
5. Add mention of `cargo blox generate` in Development Workflow section
6. Resolve `#[self_id]`/`#[provides]`/`#[ctor]` deprecation contradiction → keep them as backward-compatible but document convention-based as primary
7. Add `bloxide-codegen` and `cargo-blox` to the crate listing

### QUICK_REFERENCE.md
1. Replace `#[self_id]` annotation reference with "Auto-detected from `self_id: ActorId` field"
2. Replace `#[ctor]` annotation reference with "Auto-detected by naming convention (`foo_factory` fields)"

## Group B: Build Guides and Architecture Specs

### skills/building-with-bloxide/reference.md
Rewrite three major sections:
- `blox_messages!` → `blox.toml` `[[messages]]` schema
- `event!` → `blox.toml` `[event]` schema  
- `#[derive(StateTopology)]` → `blox.toml` `[topology]` schema
- Update `#[ctor]` section to match current policy (convention-based primary)

### skills/contributing-to-bloxide/SKILL.md
1. Update bloxide-macros crate map → remove `StateTopology`
2. Update Key Macros section → remove `StateTopology`, `event!`, `blox_messages!`; add `bloxide-codegen`, `cargo-blox`

### spec/architecture/*.md
1. **00-layered-architecture.md**: Remove `SupervisedRunLoop` from Tier 2 traits; add `bloxide-codegen` as a tool
2. **01-system-architecture.md**: Remove `SupervisedRunLoop` references
3. **02-hsm-engine.md**: Replace `#[derive(StateTopology)]` examples with `blox.toml` + generated code; fix lifecycle examples to use `dispatch(LifecycleCommand::Start)` not `machine.start()`
4. **04-static-wiring.md**: Remove `machine.start()` references
5. **07-typed-mailboxes.md**: Replace `bloxide_macros::mailboxes_impls!(16)` with "generated by bloxide-codegen"; replace `#[blox_event]` with `blox.toml` `[event]`
6. **08-supervision.md**: Remove all `SupervisedRunLoop` trait references; update sequence diagrams to show `dispatch(LifecycleCommand::Start)`; fix `run_supervised_actor` as a function not a trait
7. **10-effects-and-capabilities.md**: Fix test pattern to use `dispatch()` not `machine.start()`
8. **11-dynamic-actors.md**: Replace `#[blox_event]` with `blox.toml` `[event]` with `generics`; remove `DynamicActorSupport` references
9. **12-action-crate-pattern.md**: Update examples to use convention-based detection (no `#[self_id]`, `#[provides]`)
10. **13-factory-injection-and-supervision.md**: Remove `#[ctor]` references; update two-stream model description to unified lifecycle

## Group C: Blox Specs and Templates

### spec/bloxes/*.md (ping.md, pong.md, counter.md, pool.md, worker.md)
1. Replace all `machine.start()` and `machine.reset()` in state diagrams with `dispatch(Start)` and `dispatch(Reset)`
2. Update acceptance criteria and test mappings to use `dispatch(LifecycleCommand::Start)` / `dispatch(LifecycleCommand::Reset)`

### spec/templates/blox-spec.md
1. Same fixes as blox specs
2. Remove `#[self_id]`, `#[provides]` from template examples
3. Add `blox.toml` section to template

### spec/examples/ping-spec-analysis.md
1. Replace `#[derive(StateTopology)]` with "Generated by bloxide-codegen"

### tools/bloxide-viz-export/README.md
1. Replace `#[derive(StateTopology)]` scanner with `blox.toml` `[topology]` scanner

## Group D: Source Code Doc Comments and Annotations

### bloxide-macros/src/lib.rs
1. Rewrite module-level docs: remove `StateTopology`, `EventTag`, `blox_event`, `mailboxes_impls!` from "Available macros" list
2. Fix line 5: `transitions!` is a proc macro in bloxide-macros, not a declarative macro in bloxide-core
3. Rewrite `BloxCtx` derive docs: lead with convention-based detection, show `#[delegates(...)]` only annotation; mention old annotations as backward-compatible

### bloxide-core/src/mailboxes.rs
1. Fix lines 15-16: replace "managed externally via `machine.start()` and `machine.reset()`" with "managed via `dispatch(LifecycleCommand::Start)` / `dispatch(Reset)` at VirtualRoot level"

### bloxide-core/src/accessor.rs
1. Fix line 10: replace `#[self_id]` and `#[provides]` references with convention-based detection description

### bloxide-timer/src/actions.rs
1. Fix line 8: replace `#[provides(HasTimerRef<R>)]` with "auto-detected from `timer_ref: ActorRef<TimerCommand, R>` field"

### bloxide-supervisor/src/supervisor.rs
1. Remove `#[self_id]` and `#[provides]` annotations from `SupervisorCtx`; rely on convention detection or manual impl

### bloxides/pool/src/ctx.rs
1. Remove `#[ctor]` annotations; rename `self_ref` if it should not generate `HasSelfRef`, or keep and rely on convention

### cargo-blox/src/new.rs
1. Remove `codegen` feature gate from scaffolded crates; generated modules are unconditional

### bloxide-macros/src/blox_ctx/analyze.rs
1. Fix error message at line 307: remove `#[provides]` from example, use convention-based description

## Group E: Generated Files and Examples

### Generated file headers
1. Make `mailboxes_impls.rs` header consistent: single-line `Auto-generated by bloxide-codegen` header, remove copyright line from codegen tool

### Example comments
1. Fix tokio-pool-demo.rs KillCap comment: clarify it's infrastructure setup, not an active demonstration

### Crate naming
1. Document that `embassy-demo-impl` is shared by both runtimes (or note in README/AGENTS.md)
