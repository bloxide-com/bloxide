# Supervisor (deprecated blox spec)

The supervisor is no longer a per-project blox. It is now a generic, reusable library component provided by `crates/bloxide-supervisor/`.

See `spec/architecture/08-supervision.md` for the full design, including `SupervisorSpec<R>`, `ChildPolicy`, `GroupShutdown`, and wiring examples.
