#!/bin/bash
set -e

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"
cargo build -p cargo-blox --quiet

BLOX="$REPO_ROOT/target/debug/cargo-blox blox"

DEMO="demo/tokio-demo"
rm -rf "$REPO_ROOT/$DEMO"
mkdir -p "$REPO_ROOT/$DEMO"
cd "$REPO_ROOT/$DEMO"

# ── Create workspace Cargo.toml (minimal, members added later) ──────────────
cat > Cargo.toml <<'WORKSPACE'
[workspace]
members = [
    "crates/messages/ping-pong-messages",
    "crates/actions/ping-pong-actions",
    "crates/bloxes/ping",
    "crates/bloxes/pong",
    "apps/tokio-demo",
]
resolver = "2"

[workspace.package]
version = "0.0.3"
edition = "2021"
license = "MIT"
repository = "https://github.com/bloxide-com/bloxide"

[workspace.dependencies]
bloxide-core         = { path = "../../crates/bloxide-core" }
bloxide-tokio        = { path = "../../runtimes/bloxide-tokio" }
bloxide-macros       = { path = "../../crates/bloxide-macros" }
bloxide-log          = { path = "../../crates/bloxide-log", features = ["log"] }
bloxide-timer        = { path = "../../crates/bloxide-timer" }
ping-pong-messages   = { path = "crates/messages/ping-pong-messages" }
ping-pong-actions    = { path = "crates/actions/ping-pong-actions" }
ping-blox            = { path = "crates/bloxes/ping" }
pong-blox            = { path = "crates/bloxes/pong" }
tokio                = { version = "1", features = ["full"] }
tracing              = "0.1"
tracing-subscriber   = { version = "0.3", features = ["env-filter"] }
tracing-log          = "0.2"

[profile.dev]
panic = "abort"
WORKSPACE

# ── Layer 1: Messages ────────────────────────────────────────────────────────
mkdir -p crates/messages/ping-pong-messages/src/generated

cat > crates/messages/ping-pong-messages/Cargo.toml <<'EOF'
[package]
name = "ping-pong-messages"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
bloxide-macros = { workspace = true }
EOF

cat > crates/messages/ping-pong-messages/blox.toml <<'EOF'
[[messages]]
name = "PingPongMsg"
visibility = "pub"

[[messages.variants]]
name = "Ping"

[[messages.variants.fields]]
name = "round"
ty = "u32"

[[messages.variants]]
name = "Pong"

[[messages.variants.fields]]
name = "round"
ty = "u32"

[[messages.variants]]
name = "Resume"
EOF

cat > crates/messages/ping-pong-messages/src/lib.rs <<'EOF'
#![no_std]

pub mod generated;

pub mod prelude {
    pub use crate::*;
}

pub use generated::*;
EOF

cat > crates/messages/ping-pong-messages/src/generated/mod.rs <<'EOF'
// Auto-generated module.
// Files in this directory are generated by bloxide-codegen.
EOF

# ── Layer 2: Actions ────────────────────────────────────────────────────────
mkdir -p crates/actions/ping-pong-actions/src

cat > crates/actions/ping-pong-actions/Cargo.toml <<'EOF'
[package]
name = "ping-pong-actions"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
bloxide-core      = { workspace = true }
bloxide-log       = { workspace = true }
bloxide-macros    = { workspace = true }
bloxide-timer     = { workspace = true }
ping-pong-messages = { workspace = true }
EOF

cat > crates/actions/ping-pong-actions/src/lib.rs <<'ACTIONS'
#![no_std]

use bloxide_core::{
    accessor::HasSelfId, capability::BloxRuntime, messaging::ActorRef, transition::ActionResult,
};
use bloxide_macros::delegatable;
use bloxide_timer::{cancel_timer, set_timer, HasTimerRef, TimerId};
use ping_pong_messages::{Ping, PingPongMsg, Pong, Resume};

pub trait HasPeerRef<R: BloxRuntime> {
    fn peer_ref(&self) -> &ActorRef<PingPongMsg, R>;
}

pub trait HasSelfRef<R: BloxRuntime> {
    fn self_ref(&self) -> &ActorRef<PingPongMsg, R>;
}

#[delegatable]
pub trait CountsRounds {
    type Round: Copy + PartialEq + PartialOrd
        + core::ops::Add<Output = Self::Round>
        + From<u8> + core::fmt::Display;
    fn round(&self) -> Self::Round;
    fn set_round(&mut self, round: Self::Round);
}

#[delegatable]
pub trait HasCurrentTimer {
    fn current_timer(&self) -> Option<TimerId>;
    fn set_current_timer(&mut self, timer: Option<TimerId>);
}

pub fn increment_round<C: CountsRounds>(ctx: &mut C) {
    let one = C::Round::from(1);
    ctx.set_round(ctx.round() + one);
}

pub fn send_initial_ping<R, C>(ctx: &mut C)
where
    R: BloxRuntime,
    C: HasSelfId + HasPeerRef<R> + CountsRounds,
    C::Round: Into<u32>,
{
    if ctx.round() == C::Round::from(1) {
        if ctx
            .peer_ref()
            .try_send(
                ctx.self_id(),
                PingPongMsg::Ping(Ping { round: ctx.round().into() }),
            )
            .is_err()
        {
            bloxide_log::blox_log_warn!(
                ctx.self_id(),
                "send_initial_ping: peer channel full, first Ping dropped"
            );
        }
    }
}

pub fn send_ping<R, C>(ctx: &mut C) -> ActionResult
where
    R: BloxRuntime,
    C: HasSelfId + HasPeerRef<R> + CountsRounds,
    C::Round: Into<u32>,
{
    ActionResult::from(ctx.peer_ref().try_send(
        ctx.self_id(),
        PingPongMsg::Ping(Ping { round: ctx.round().into() }),
    ))
}

pub fn send_pong<R, C>(ctx: &mut C, ping: &Ping) -> ActionResult
where
    R: BloxRuntime,
    C: HasSelfId + HasPeerRef<R>,
{
    ActionResult::from(
        ctx.peer_ref()
            .try_send(ctx.self_id(), PingPongMsg::Pong(Pong { round: ping.round })),
    )
}

pub fn schedule_resume<R, C>(ctx: &mut C, duration_ms: u64)
where
    R: BloxRuntime,
    C: HasSelfRef<R> + HasTimerRef<R> + HasSelfId + HasCurrentTimer,
{
    let id = set_timer::<R, C, PingPongMsg>(
        ctx, duration_ms, ctx.self_ref(), PingPongMsg::Resume(Resume),
    );
    ctx.set_current_timer(Some(id));
}

pub fn cancel_current_timer<R, C>(ctx: &mut C)
where
    R: BloxRuntime,
    C: HasSelfId + HasTimerRef<R> + HasCurrentTimer,
{
    if let Some(id) = ctx.current_timer() {
        cancel_timer::<R, C>(ctx, id);
        ctx.set_current_timer(None);
    }
}
ACTIONS

# ── Layer 4: Blox — Ping ───────────────────────────────────────────────────
mkdir -p crates/bloxes/ping/src/generated

cat > crates/bloxes/ping/Cargo.toml <<'EOF'
[package]
name = "ping-blox"
version.workspace = true
edition.workspace = true
publish = false

[features]
default = ["std"]
std = ["bloxide-core/std", "bloxide-timer/std"]

[dependencies]
bloxide-core        = { workspace = true, features = ["alloc"] }
bloxide-macros      = { workspace = true }
bloxide-log         = { workspace = true }
bloxide-timer       = { workspace = true }
ping-pong-actions   = { workspace = true }
ping-pong-messages  = { workspace = true }

[dev-dependencies]
bloxide-core        = { workspace = true, features = ["std"] }
EOF

cat > crates/bloxes/ping/blox.toml <<'EOF'
[actor]
name = "Ping"

[event]
name = "PingEvent"

[[event.mailboxes]]
variant = "Msg"
message = "PingPongMsg"
message_path = "ping_pong_messages::PingPongMsg"

[context]
name = "PingCtx"
generics = "<R: BloxRuntime, B: HasCurrentTimer + CountsRounds>"
actions_crate = "ping_pong_actions"

[[context.fields]]
name = "self_id"
ty = "ActorId"

[[context.fields]]
name = "peer_ref"
ty = "ActorRef<PingPongMsg, R>"

[[context.fields]]
name = "self_ref"
ty = "ActorRef<PingPongMsg, R>"

[[context.fields]]
name = "timer_ref"
ty = "ActorRef<TimerCommand, R>"

[[context.fields]]
name = "behavior"
ty = "B"
delegates = ["HasCurrentTimer", "CountsRounds"]

[topology]
handler_fns = ["OPERATING_FNS", "ACTIVE_FNS", "PAUSED_FNS", "DONE_FNS", "ERROR_FNS"]

[[topology.states]]
name = "Operating"
composite = true

[[topology.states]]
name = "Active"
parent = "Operating"

[[topology.states]]
name = "Paused"
parent = "Operating"

[[topology.states]]
name = "Done"
terminal = true

[[topology.states]]
name = "Error"
error = true
EOF

cat > crates/bloxes/ping/src/lib.rs <<'PING_LIB'
#![no_std]

#[cfg(feature = "std")]
extern crate std;

pub mod generated;
pub mod actions;
pub mod prelude;

#[cfg(all(test, feature = "std"))]
mod tests;

pub use generated::*;

pub const MAX_ROUNDS: u8 = 5;
pub const PAUSE_AT_ROUND: u8 = 2;
pub const PAUSE_DURATION_MS: u64 = 150;
PING_LIB

cat > crates/bloxes/ping/src/prelude.rs <<'EOF'
pub use crate::{PingCtx, PingEvent, PingSpec, PingState};
EOF

cat > crates/bloxes/ping/src/tests.rs <<'EOF'
EOF

cat > crates/bloxes/ping/src/generated/mod.rs <<'EOF'
// Auto-generated module.
// Files in this directory are generated by bloxide-codegen.
EOF

cat > crates/bloxes/ping/src/actions.rs <<'PING_ACTIONS'
use crate::{PingCtx, PingEvent, PingSpec, PingState, MAX_ROUNDS, PAUSE_AT_ROUND, PAUSE_DURATION_MS};
use bloxide_core::{
    capability::BloxRuntime, spec::StateFns, transition::ActionResult, transitions,
};
use ping_pong_actions::{
    cancel_current_timer, increment_round, schedule_resume, send_initial_ping, send_ping,
    CountsRounds, HasCurrentTimer,
};
use ping_pong_messages::PingPongMsg;

impl<R, B> PingSpec<R, B>
where
    R: BloxRuntime,
    B: HasCurrentTimer + CountsRounds + Default + 'static,
    B::Round: Into<u32>,
{
    fn forward_ping(ctx: &mut PingCtx<R, B>, _ev: &PingEvent) -> ActionResult {
        send_ping::<R, _>(ctx)
    }

    fn schedule_pause_timer(ctx: &mut PingCtx<R, B>) {
        schedule_resume::<R, _>(ctx, PAUSE_DURATION_MS);
    }

    fn cancel_pause_timer(ctx: &mut PingCtx<R, B>) {
        cancel_current_timer::<R, _>(ctx);
    }

    pub(crate) const OPERATING_FNS: StateFns<Self> = StateFns {
        on_entry: &[],
        on_exit: &[],
        transitions: transitions![
            PingPongMsg::Pong(_) => stay,
        ],
    };

    pub(crate) const ACTIVE_FNS: StateFns<Self> = StateFns {
        on_entry: &[increment_round, send_initial_ping],
        on_exit: &[],
        transitions: transitions![
            PingPongMsg::Pong(_) => {
                actions [Self::forward_ping]
                guard(ctx, _results) {
                    ctx.round() >= B::Round::from(MAX_ROUNDS)     => PingState::Done,
                    ctx.round() == B::Round::from(PAUSE_AT_ROUND) => PingState::Paused,
                    _                                             => PingState::Active,
                }
            },
        ],
    };

    pub(crate) const PAUSED_FNS: StateFns<Self> = StateFns {
        on_entry: &[Self::schedule_pause_timer],
        on_exit: &[Self::cancel_pause_timer],
        transitions: transitions![
            PingPongMsg::Resume(_resume) => {
                actions [Self::forward_ping]
                transition PingState::Active
            },
        ],
    };

    pub(crate) const DONE_FNS: StateFns<Self> = StateFns {
        on_entry: &[],
        on_exit: &[],
        transitions: &[],
    };

    pub(crate) const ERROR_FNS: StateFns<Self> = StateFns {
        on_entry: &[],
        on_exit: &[],
        transitions: &[],
    };
}
PING_ACTIONS

# ── Layer 4: Blox — Pong ───────────────────────────────────────────────────
mkdir -p crates/bloxes/pong/src/generated

cat > crates/bloxes/pong/Cargo.toml <<'EOF'
[package]
name = "pong-blox"
version.workspace = true
edition.workspace = true
publish = false

[features]
default = ["std"]
std = ["bloxide-core/std"]

[dependencies]
bloxide-core        = { workspace = true, features = ["alloc"] }
bloxide-macros      = { workspace = true }
bloxide-log         = { workspace = true }
ping-pong-actions   = { workspace = true }
ping-pong-messages  = { workspace = true }

[dev-dependencies]
bloxide-core        = { workspace = true, features = ["std"] }
EOF

cat > crates/bloxes/pong/blox.toml <<'EOF'
[actor]
name = "Pong"

[event]
name = "PongEvent"

[[event.mailboxes]]
variant = "Msg"
message = "PingPongMsg"
message_path = "ping_pong_messages::PingPongMsg"

[context]
name = "PongCtx"
generics = "<R: BloxRuntime>"
actions_crate = "ping_pong_actions"

[[context.fields]]
name = "self_id"
ty = "ActorId"

[[context.fields]]
name = "peer_ref"
ty = "ActorRef<PingPongMsg, R>"

[topology]
handler_fns = ["READY_FNS"]

[[topology.states]]
name = "Ready"
EOF

cat > crates/bloxes/pong/src/lib.rs <<'PONG_LIB'
#![no_std]

#[cfg(feature = "std")]
extern crate std;

pub mod generated;
pub mod actions;
pub mod prelude;

#[cfg(all(test, feature = "std"))]
mod tests;

pub use generated::*;
PONG_LIB

cat > crates/bloxes/pong/src/prelude.rs <<'EOF'
pub use crate::{PongCtx, PongEvent, PongSpec, PongState};
EOF

cat > crates/bloxes/pong/src/tests.rs <<'EOF'
EOF

cat > crates/bloxes/pong/src/generated/mod.rs <<'EOF'
// Auto-generated module.
// Files in this directory are generated by bloxide-codegen.
EOF

cat > crates/bloxes/pong/src/actions.rs <<'PONG_ACTIONS'
use crate::prelude::*;
use bloxide_core::{capability::BloxRuntime, spec::StateFns, transition::ActionResult, transitions};
use ping_pong_actions::send_pong;
use ping_pong_messages::PingPongMsg;

impl<R: BloxRuntime> PongSpec<R> {
    fn reply_pong_action(ctx: &mut PongCtx<R>, ev: &PongEvent) -> ActionResult {
        if let Some(PingPongMsg::Ping(ping)) = ev.msg_payload() {
            return send_pong::<R, _>(ctx, ping);
        }
        ActionResult::Ok
    }

    pub const READY_FNS: StateFns<Self> = StateFns {
        on_entry: &[],
        on_exit: &[],
        transitions: transitions![
            PingPongMsg::Ping(_) => {
                actions [Self::reply_pong_action]
                stay
            },
        ],
    };
}
PONG_ACTIONS

# ── Generate all boilerplate from TOML ───────────────────────────────────────
$BLOX generate

# ── Patch generated spec_skeleton.rs and ctx.rs (codegen has known bugs) ──────

# Ping spec_skeleton.rs — codegen gets generics and ctx type wrong
cat > crates/bloxes/ping/src/generated/spec_skeleton.rs <<'PING_SPEC'
// Auto-generated by bloxide-codegen. Do not edit manually.
use core::marker::PhantomData;
use bloxide_core::{
    capability::BloxRuntime,
    spec::{MachineSpec, StateFns},
    HasSelfId,
};
use ping_pong_actions::{HasCurrentTimer, CountsRounds};
use ping_pong_messages::PingPongMsg;

use crate::{PingCtx, PingEvent};
pub use crate::generated::topology::PingState;

pub struct PingSpec<R: BloxRuntime, B: HasCurrentTimer + CountsRounds>(PhantomData<(R, B)>);

impl<R, B> MachineSpec for PingSpec<R, B>
where
    R: BloxRuntime,
    B: HasCurrentTimer + CountsRounds + Default + 'static,
    B::Round: Into<u32>,
{
    type State = PingState;
    type Event = PingEvent;
    type Ctx = PingCtx<R, B>;
    type Mailboxes<Rt: BloxRuntime> = (Rt::Stream<PingPongMsg>,);

    const HANDLER_TABLE: &'static [&'static StateFns<Self>] = ping_state_handler_table!(Self);

    fn initial_state() -> PingState {
        PingState::Active
    }

    fn is_terminal(state: &PingState) -> bool {
        matches!(state, PingState::Done)
    }

    fn is_error(state: &PingState) -> bool {
        matches!(state, PingState::Error)
    }

    fn on_init_entry(ctx: &mut PingCtx<R, B>) {
        ctx.behavior = B::default();
        bloxide_log::blox_log_info!(ctx.self_id(), "reset — behavior cleared");
    }
}
PING_SPEC

# Ping ctx.rs — codegen misses message/timer imports
cat > crates/bloxes/ping/src/generated/ctx.rs <<'PING_CTX'
// Auto-generated by bloxide-codegen. Do not edit manually.
use bloxide_core::{ActorId, capability::BloxRuntime, messaging::ActorRef};
use bloxide_macros::BloxCtx;
use bloxide_timer::{HasTimerRef, TimerCommand, TimerId};
use ping_pong_actions::{
    CountsRounds, HasCurrentTimer, HasPeerRef, HasSelfRef,
    __delegate_CountsRounds, __delegate_HasCurrentTimer,
};
use ping_pong_messages::PingPongMsg;

#[derive(BloxCtx)]
pub struct PingCtx<R: BloxRuntime, B: HasCurrentTimer + CountsRounds> {
    pub self_id: ActorId,
    pub peer_ref: ActorRef<PingPongMsg, R>,
    pub self_ref: ActorRef<PingPongMsg, R>,
    pub timer_ref: ActorRef<TimerCommand, R>,
    #[delegates(HasCurrentTimer, CountsRounds)]
    pub behavior: B,
}
PING_CTX

# Pong spec_skeleton.rs — codegen adds wrong B generic
cat > crates/bloxes/pong/src/generated/spec_skeleton.rs <<'PONG_SPEC'
// Auto-generated by bloxide-codegen. Do not edit manually.
use core::marker::PhantomData;
use bloxide_core::{
    capability::BloxRuntime,
    spec::{MachineSpec, StateFns},
};
use ping_pong_messages::PingPongMsg;

use crate::PongCtx;
use crate::PongEvent;
pub use crate::generated::topology::PongState;

pub struct PongSpec<R: BloxRuntime>(PhantomData<R>);

impl<R: BloxRuntime> MachineSpec for PongSpec<R> {
    type State = PongState;
    type Event = PongEvent;
    type Ctx = PongCtx<R>;
    type Mailboxes<Rt: BloxRuntime> = (Rt::Stream<PingPongMsg>,);

    const HANDLER_TABLE: &'static [&'static StateFns<Self>] = pong_state_handler_table!(Self);

    fn initial_state() -> PongState {
        PongState::Ready
    }

    fn on_init_entry(_ctx: &mut PongCtx<R>) {}
}
PONG_SPEC

# Pong ctx.rs — codegen adds wrong B generic and misses message imports
cat > crates/bloxes/pong/src/generated/ctx.rs <<'PONG_CTX'
// Auto-generated by bloxide-codegen. Do not edit manually.
use bloxide_core::{capability::BloxRuntime, ActorId, ActorRef};
use bloxide_macros::BloxCtx;
use ping_pong_actions::HasPeerRef;
use ping_pong_messages::PingPongMsg;

#[derive(BloxCtx)]
pub struct PongCtx<R: BloxRuntime> {
    pub self_id: ActorId,
    pub peer_ref: ActorRef<PingPongMsg, R>,
}
PONG_CTX

# ── Layer 5: Binary ──────────────────────────────────────────────────────────
mkdir -p apps/tokio-demo/src

cat > apps/tokio-demo/Cargo.toml <<'CRATE'
[package]
name = "tokio-demo"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
bloxide-core       = { workspace = true, features = ["std"] }
bloxide-tokio      = { workspace = true }
bloxide-log        = { workspace = true }
bloxide-timer      = { workspace = true, features = ["std"] }
ping-blox          = { workspace = true }
pong-blox          = { workspace = true }
ping-pong-messages = { workspace = true }
ping-pong-actions  = { workspace = true }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-log = "0.2"
CRATE

cat > apps/tokio-demo/src/main.rs <<'MAIN'
use bloxide_core::lifecycle::LifecycleCommand;
use bloxide_tokio::prelude::*;
use ping_blox::prelude::*;
use ping_pong_messages::prelude::*;
use ping_pong_actions::{CountsRounds, HasCurrentTimer};
use pong_blox::prelude::*;
use std::time::Duration;

#[derive(Debug, Default, Clone)]
struct DemoBehavior {
    round: u32,
    current_timer: Option<bloxide_timer::TimerId>,
}

impl CountsRounds for DemoBehavior {
    type Round = u32;
    fn round(&self) -> u32 { self.round }
    fn set_round(&mut self, r: u32) { self.round = r; }
}

impl HasCurrentTimer for DemoBehavior {
    fn current_timer(&self) -> Option<bloxide_timer::TimerId> { self.current_timer }
    fn set_current_timer(&mut self, t: Option<bloxide_timer::TimerId>) { self.current_timer = t; }
}

bloxide_tokio::actor_task_supervised!(ping_task, PingSpec<TokioRuntime, DemoBehavior>);
bloxide_tokio::actor_task_supervised!(pong_task, PongSpec<TokioRuntime>);
bloxide_tokio::root_task!(supervisor_task, SupervisorSpec<TokioRuntime>);

#[tokio::main]
async fn main() {
    tracing_log::LogTracer::init().ok();
    tracing_subscriber::fmt()
        .with_env_filter(
            tracing_subscriber::EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| tracing_subscriber::EnvFilter::new("info")),
        )
        .try_init()
        .ok();

    let timer_ref = bloxide_tokio::spawn_timer!(8);

    let ((ping_ref,), ping_mbox) = bloxide_tokio::channels! {
        PingPongMsg(16),
    };
    let ping_id = ping_ref.id();

    let ((pong_ref,), pong_mbox) = bloxide_tokio::channels! {
        PingPongMsg(16),
    };
    let pong_id = pong_ref.id();

    let ping_ctx = PingCtx::new(
        ping_id,
        pong_ref.clone(),
        ping_ref.clone(),
        timer_ref,
        DemoBehavior::default(),
    );
    let pong_ctx = PongCtx::new(pong_id, ping_ref.clone());

    let ping_machine = StateMachine::new(ping_ctx);
    let pong_machine = StateMachine::new(pong_ctx);

    let mut group = ChildGroupBuilder::new(GroupShutdown::WhenAnyDone);
    bloxide_tokio::spawn_child!(
        group,
        ping_task(ping_machine, ping_mbox, ping_id),
        ChildPolicy::Restart { max: 1 }
    );
    bloxide_tokio::spawn_child!(
        group,
        pong_task(pong_machine, pong_mbox, pong_id),
        ChildPolicy::Stop
    );
    let sup_control_ref = group.control_ref();
    let sup_notify = group.notify_sender();
    let sup_id = bloxide_tokio::next_actor_id!();
    let (children, sup_notify_rx, sup_control_rx) = group.finish();

    let sup_ctx = SupervisorCtx::new(sup_id, children);
    let mut sup_machine = StateMachine::<SupervisorSpec<TokioRuntime>>::new(sup_ctx);
    sup_machine.dispatch(SupervisorEvent::<TokioRuntime>::Lifecycle(
        LifecycleCommand::Start,
    ));

    let health_ref = sup_control_ref.clone();
    let _health_task = tokio::spawn(async move {
        let mut ticker = tokio::time::interval(Duration::from_millis(500));
        loop {
            ticker.tick().await;
            if health_ref
                .try_send(sup_id, SupervisorControl::HealthCheckTick)
                .is_err()
            {
                break;
            }
        }
    });

    let ((pong2_ref,), pong2_mbox) = bloxide_tokio::channels! {
        PingPongMsg(16),
    };
    let pong2_id = pong2_ref.id();
    let pong2_ctx = PongCtx::new(pong2_id, ping_ref);
    let pong2_machine = StateMachine::new(pong2_ctx);
    bloxide_tokio::spawn_child_dynamic!(
        sup_id,
        sup_control_ref,
        sup_notify,
        pong_task(pong2_machine, pong2_mbox, pong2_id),
        ChildPolicy::Stop
    )
    .expect("supervisor control channel should accept dynamic registration");

    supervisor_task(sup_machine, (sup_notify_rx, sup_control_rx)).await;
    println!("tokio-demo complete");
}
MAIN

cargo run -p tokio-demo
