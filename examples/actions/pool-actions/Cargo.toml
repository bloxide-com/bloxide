[package]
name = "pool-actions"
version.workspace = true
edition.workspace = true
description = "Domain action traits and generic functions for the worker pool example"

[features]
default = []
std = ["bloxide-core/std", "bloxide-spawn/std"]

[dependencies]
bloxide-core  = { path = "../../../crates/bloxide-core", features = ["alloc"] }
bloxide-spawn = { path = "../../../crates/bloxide-spawn" }
pool-messages = { path = "../../messages/pool-messages" }
